if SHIELD_SCORN_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Scorn"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_SCORN_LEFT || SHIELD_SCORN_RIGHT

config ZMK_SPLIT
    default y

endif